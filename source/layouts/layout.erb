<!DOCTYPE html>
<html>
<head>
  <%= partial "/layouts/partials/head" %>
  <!-- Use the title from a page's frontmatter if it has one -->
  <title><%= current_page.data.title || "jacquesite" %></title>
</head>

<body>
  <%= partial "/layouts/partials/banner" %>
  <div id="main">
    <%= yield %>
  </div>
  <script>
  <%= partial "/layouts/partials/menuscripts" %>
  </script>
</body>
</html>
